function s = whichInspectGUI(obj)
%whichInspectGUI Returns 1 if there is a specific GUI for the object
%   S = whichInspectGUI(OBJ) returns 0 if the object uses the generic 
%   InspectGUI interface and 1 if the object uses a specific InspectGUI
%   interface.
%
%   Dependencies: None.

s = 0;
